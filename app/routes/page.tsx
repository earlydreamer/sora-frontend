"use client";

// TODO: 경로 설정 화면
// - 경로 이름, 출발지, 도착지, 도착 희망 시각, 개인 버퍼 입력
// - 최대 2개 경로 등록
// - 저장 → POST /routes
// - 수정 → PATCH /routes/:id
// - 삭제 → DELETE /routes/:id
//
// 구현: Codex 위임 대상

export default function RoutesPage() {
  return (
    <main className="flex flex-col min-h-screen p-6 gap-6">
      <h1 className="text-xl font-bold">경로 설정</h1>
      {/* TODO: 구현 */}
    </main>
  );
}
