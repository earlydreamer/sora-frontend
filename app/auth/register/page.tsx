"use client";

// TODO: 회원가입 화면
// - 이메일 / 비밀번호 / 비밀번호 확인 입력
// - POST /auth/register → access_token → localStorage 저장 → /routes로 (첫 경로 설정 유도)
// - 로그인 링크 → /auth/login
//
// 구현: Codex 위임 대상

export default function RegisterPage() {
  return (
    <main className="flex flex-col items-center justify-center min-h-screen p-6 gap-6">
      <h1 className="text-xl font-bold">회원가입</h1>
      {/* TODO: 구현 */}
    </main>
  );
}
